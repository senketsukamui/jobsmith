{
  "name": "job-tracker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --filter @job-tracker/desktop dev",
    "build": "pnpm --filter @job-tracker/desktop build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @job-tracker/desktop db:generate",
    "db:migrate": "pnpm --filter @job-tracker/desktop db:migrate"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "esbuild", "@libsql/client", "@libsql/libsql-darwin-arm64"]
  }
}
